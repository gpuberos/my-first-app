import Wrapper from "../Wrapper";

const index = () => {
    return (
        <Wrapper>
            <h1 className="h3 text-primary">Welcome to the homepage</h1>
        </Wrapper>
    );
};

export default index;