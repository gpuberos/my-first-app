import Wrapper from "../Wrapper";

const index = () => {
    return (
        <Wrapper>
            <h1 className="h3 text-primary">Nos Prestations</h1>
        </Wrapper>
    );
};

export default index;