import Wrapper from "../Wrapper";
import Lifecycle from "./Lifecycle";

const index = () => {
    return (
        <Wrapper>
            <Lifecycle name={"Doe"} />
        </Wrapper>
    );
};

export default index;