import Wrapper from "../Wrapper";
import Lifecycle from "./Lifecycle";

const index = () => {
    return (
        <Wrapper>
            <Lifecycle />
        </Wrapper>
    );
};

export default index;