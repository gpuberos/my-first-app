import Wrapper from "../Wrapper";

const index = () => {
    return (
        <Wrapper>
            <h1 className="h3 text-primary">Notre Entreprise</h1>
        </Wrapper>
    );
};

export default index;